package cs3500.animator.provider.controller;

/**
 * Interface that represents the controller for the easy animator. Allows the user to play an
 * animation.
 */
public interface IEasyAnimatorController {
  /**
   * Start the program, i.e. gives control to the controller.
   */
  void animate();
}
